[user]
	# 環境変数で定義
	#  GIT_AUTHOR_NAME
	#  GIT_AUTHOR_EMAIL
	name = jimii
	email = jimi024lab@gmail.com
[core]
	editor = code  --wait
	quotepath = false
	ignorecase = false
	pager = delta
[color]
	ui = true
[ghq]
	root = /Users/jimii/src
[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection
    side-by-side = true
[merge]
    conflictstyle = zdiff3

